name: flutter_imc_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  firebase_core: ^2.25.0
  firebase_auth: ^4.17.8
  firebase_database: ^10.5.0
  cloud_firestore: ^4.17.5
  barcode_scan2: ^4.2.0
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
